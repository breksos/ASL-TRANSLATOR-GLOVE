name: asl_glove
description: ASL Sign Language Glove Translator — GTU CSE496 Graduation Project
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.31.0
  tflite_flutter: ^0.11.0
  flutter_tts: ^3.8.5
  permission_handler: ^11.0.0
  shared_preferences: ^2.2.0
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/models/
    - assets/normalization/
